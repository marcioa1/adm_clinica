require 'spec_helper'

describe Alta do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Alta.create!(@valid_attributes)
  end
end
