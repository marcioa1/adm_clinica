- content_for :main do
  %p
    &nbsp;
    = render :partial => 'cheques/detalhe_cheque' , :locals => {:cheque => @cheque }
  
  - form_for @acompanhamento_cheque do |f| 
    = f.hidden_field :cheque_id
    %p
      = f.label :descrição
      = ':'
      = f.text_area :descricao, :size=>"70x3"
    %p
      = f.submit 'Salvar' 
